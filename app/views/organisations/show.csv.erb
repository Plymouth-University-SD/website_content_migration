"Label","Number"
"New",<%= @counts["new"].nil? ? "0" : "#{@counts["new"]}"%>
"Review",<%= @counts["review"].nil? ? "0" : "#{@counts["review"]}"%>
"Update",<%= @counts["update"].nil? ? "0" : "#{@counts["update"]}"%>
"Updating",<%= @counts["updating"].nil? ? "0" : "#{@counts["updating"]}"%>
"Complete",<%= @pages_completed.nil? ? "0" : "#{@pages_completed}"%>